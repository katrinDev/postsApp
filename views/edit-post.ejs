<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head.ejs') %>
<body>
  <%- include('./partials/nav.ejs') %>
  <form id="post-form" method="post" action="/edit/<%= post.id%>?_method=PUT" > 
    <!-- action содержит route, который и будет обрабатывать получаемые из формы данные --> 
    <div class="form-info">
      <label>Post Title
        <!-- данный шаблон в отличие от add-post принимает первоначальные данные редактируемой новости -->
        <input type="text" name="title" value="<%= post.title %>">
      </label>
      <label>Author
        <input type="text" name="author" value="<%= post.author%>">
      </label>
    </div>
    <div class="form-text">
      <label>Post Text
        <textarea name="text"><%= post.text %></textarea>
      </label>
    </div>
    <div class="form-button">
      <input type="submit" value="Submit">
    </div>
  </form>
</body>
</html>
